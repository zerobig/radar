#Область СлужебныйПрограммныйИнтерфейс

Процедура ИсполняемыеСценарии() Экспорт
	
	ЮТТесты.ВТранзакции()
		.ДобавитьТест("КорректныйПример")
		.ДобавитьТест("КорректныйПримерБезАргументов")
		.ДобавитьТест("КорректныйПримерСПустымМассивомАргументов")
		.ДобавитьТест("КорректныйПримерСПереносомСтроки")
		.ДобавитьТест("КорректныйПримерСПереносомСтрокиИЗначениями")
		.ДобавитьТест("СлишкомМногоКлючей")
		.ДобавитьТест("СлишкомМногоКлючейСПереносомСтроки")
		.ДобавитьТест("СлишкомМногоКлючейСПереносомСтрокиИЗначениями")
	;
	
КонецПроцедуры

Процедура ПередКаждымТестом() Экспорт
	
	УстановитьОтключениеБезопасногоРежима(Истина);
	
	Проект = ЮТест.Данные().КонструкторОбъекта("Справочник.Радар_Проекты")
		.Записать()
	;
	Плагин = ЮТест.Данные().КонструкторОбъекта("Справочник.Радар_Плагины")
		.Установить("Владелец", Проект)
		.Установить("Путь", "D:\1cv8\radar.zerobig\bin\Плагины\СтруктураКонструкторСлишкомМногоКлючей.epf")
		.Записать()
	;

	Плагины = Радар_Кэш.Плагины(Проект);

	Парсер = Обработки.Радар_ПарсерВстроенногоЯзыка.Создать();
	Парсер.Подключить(Плагины);
	
	Контекст = ЮТест.Контекст();
	Контекст.УстановитьЗначение("Парсер", Парсер);
	
КонецПроцедуры

Процедура КорректныйПример() Экспорт
	
	ТекстМодуля = "Процедура Тест()
	|	а = Новый Структура(""Поле1, Поле2, Поле3"");
	|КонецПроцедуры";
	
	Контекст = ЮТест.Контекст();
	Парсер = Контекст.Значение("Парсер");
	
	Модуль = Парсер.Разобрать(ТекстМодуля, Неопределено);
	Парсер.Посетить(Модуль); 
	
	ТаблицаОшибок = Парсер.ТаблицаОшибок();
	
	ЮТест.ОжидаетЧто(ТаблицаОшибок)
		.ИмеетДлину(0)
	;
	
КонецПроцедуры

Процедура КорректныйПримерБезАргументов() Экспорт
	
	ТекстМодуля = "Процедура Тест()
	|	а = Новый Структура;
	|КонецПроцедуры";
	
	Контекст = ЮТест.Контекст();
	Парсер = Контекст.Значение("Парсер");
	
	Модуль = Парсер.Разобрать(ТекстМодуля, Неопределено);
	Парсер.Посетить(Модуль); 
	
	ТаблицаОшибок = Парсер.ТаблицаОшибок();
	
	ЮТест.ОжидаетЧто(ТаблицаОшибок)
		.ИмеетДлину(0)
	;
	
КонецПроцедуры

Процедура КорректныйПримерСПустымМассивомАргументов() Экспорт
	
	ТекстМодуля = "Процедура Тест()
	|	а = Новый Структура();
	|КонецПроцедуры";
	
	Контекст = ЮТест.Контекст();
	Парсер = Контекст.Значение("Парсер");
	
	Модуль = Парсер.Разобрать(ТекстМодуля, Неопределено);
	Парсер.Посетить(Модуль); 
	
	ТаблицаОшибок = Парсер.ТаблицаОшибок();
	
	ЮТест.ОжидаетЧто(ТаблицаОшибок)
		.ИмеетДлину(0)
	;
	
КонецПроцедуры

Процедура КорректныйПримерСПереносомСтроки() Экспорт
	
	ТекстМодуля = "Процедура Тест()
	|	а = Новый Структура(""Поле1, Поле2,
	|		|Поле3"");
	|КонецПроцедуры";
	
	Контекст = ЮТест.Контекст();
	Парсер = Контекст.Значение("Парсер");
	
	Модуль = Парсер.Разобрать(ТекстМодуля, Неопределено);
	Парсер.Посетить(Модуль); 
	
	ТаблицаОшибок = Парсер.ТаблицаОшибок();
	
	ЮТест.ОжидаетЧто(ТаблицаОшибок)
		.ИмеетДлину(0)
	;
	
КонецПроцедуры

Процедура КорректныйПримерСПереносомСтрокиИЗначениями() Экспорт
	
	ТекстМодуля = "Процедура Тест()
	|	а = Новый Структура(""Поле1, Поле2,
	|		|Поле3"", 1, 2, 3);
	|КонецПроцедуры";
	
	Контекст = ЮТест.Контекст();
	Парсер = Контекст.Значение("Парсер");
	
	Модуль = Парсер.Разобрать(ТекстМодуля, Неопределено);
	Парсер.Посетить(Модуль); 
	
	ТаблицаОшибок = Парсер.ТаблицаОшибок();
	
	ЮТест.ОжидаетЧто(ТаблицаОшибок)
		.ИмеетДлину(0)
	;
	
КонецПроцедуры

Процедура СлишкомМногоКлючей() Экспорт
	
	ТекстМодуля = "Процедура Тест()
	|	а = Новый Структура(""Поле1, Поле2, Поле3, Поле4"");
	|КонецПроцедуры";
	
	Контекст = ЮТест.Контекст();
	Парсер = Контекст.Значение("Парсер");
	
	Модуль = Парсер.Разобрать(ТекстМодуля, Неопределено);
	Парсер.Посетить(Модуль); 
	
	ТаблицаОшибок = Парсер.ТаблицаОшибок();
	
	ЮТест.ОжидаетЧто(ТаблицаОшибок)
		.ИмеетДлину(1)
		.Содержит(ЮТест.Предикат()
			.Реквизит("Текст").Равно("Конструктор структуры содержит более чем три ключа"))
	;
	
КонецПроцедуры

Процедура СлишкомМногоКлючейСПереносомСтроки() Экспорт
	
	ТекстМодуля = "Процедура Тест()
	|	а = Новый Структура(""Поле1, Поле2,
	|		|Поле3, Поле4"");
	|КонецПроцедуры";
	
	Контекст = ЮТест.Контекст();
	Парсер = Контекст.Значение("Парсер");
	
	Модуль = Парсер.Разобрать(ТекстМодуля, Неопределено);
	Парсер.Посетить(Модуль); 
	
	ТаблицаОшибок = Парсер.ТаблицаОшибок();
	
	ЮТест.ОжидаетЧто(ТаблицаОшибок)
		.ИмеетДлину(1)
		.Содержит(ЮТест.Предикат()
			.Реквизит("Текст").Равно("Конструктор структуры содержит более чем три ключа"))
	;
	
КонецПроцедуры

Процедура СлишкомМногоКлючейСПереносомСтрокиИЗначениями() Экспорт
	
	ТекстМодуля = "Процедура Тест()
	|	а = Новый Структура(""Поле1, Поле2,
	|		|Поле3, Поле4"", 1, 2, 3, 4);
	|КонецПроцедуры";
	
	Контекст = ЮТест.Контекст();
	Парсер = Контекст.Значение("Парсер");
	
	Модуль = Парсер.Разобрать(ТекстМодуля, Неопределено);
	Парсер.Посетить(Модуль); 
	
	ТаблицаОшибок = Парсер.ТаблицаОшибок();
	
	ЮТест.ОжидаетЧто(ТаблицаОшибок)
		.ИмеетДлину(1)
		.Содержит(ЮТест.Предикат()
			.Реквизит("Текст").Равно("Конструктор структуры содержит более чем три ключа"))
	;
	
КонецПроцедуры

#КонецОбласти
